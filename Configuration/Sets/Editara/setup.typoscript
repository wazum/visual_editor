# TODO remove, only for debugging
config.admPanel = 1

# add <editara-content-element> warpper:
tt_content.stdWrap.postUserFunc = Andersundsehr\Editara\UserFunction\TtContentStdWrapPreUserFunc->__invoke

# add parameter editara to every link in edit mode.
config.linkVars = editara
