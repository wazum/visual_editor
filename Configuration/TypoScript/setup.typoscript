# TODO(nicetohave) replace this with dedicated Event in Core? (to remove the site set)
# add <ve-content-element> wrapper:
tt_content.stdWrap.wrapContentElementsWithVeWrapper = 1

# TODO replace this with something in the core. (to remove the site set)
# add parameter editMode to every link in edit mode.
config.linkVars = editMode
